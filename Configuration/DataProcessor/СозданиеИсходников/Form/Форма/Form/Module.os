
&НаСервере
Функция КомандаВыполнитьНаСервере()
	
	Перем Объект;
	
	Объект = РеквизитФормыВЗначение("Объект");
	ПутьКФайлу = Объект.ВыполнитьСоздание();
	
	Данные = Новый ДвоичныеДанные(ПутьКФайлу);
	
	Возврат ПоместитьВоВременноеХранилище(Данные);
	
КонецФункции

&НаКлиенте
Процедура КомандаВыполнить(Команда)
	Адрес = КомандаВыполнитьНаСервере();
	ПолучитьФайл(Адрес, "enums.zip");
КонецПроцедуры
